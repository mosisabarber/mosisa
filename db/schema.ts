/**
 * Drizzle schema — single source of truth for the database (AGENTS.md §3).
 *
 * Stage 1 placeholder: the full schema (barbers, services, working_hours,
 * barber_schedules, blocked_times, appointments, staff_users) is implemented
 * in Stage 2, together with the hand-written raw SQL migration that adds the
 * `no_overlapping_appointments` EXCLUDE constraint.
 *
 * Do not add tables or columns not defined in AGENTS.md §3.
 */
export {};
